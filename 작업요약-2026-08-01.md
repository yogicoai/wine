# 작업 요약 — 2026-08-01 (자동 진행분)

> 지시하신 대로 데이터 확장 → 프론트 구성(힉스필드 제외) → 컴퓨터 종료까지 자율 진행한 기록입니다.
> 다음 세션에서 이 파일만 읽으면 어디까지 왔는지 파악됩니다.

## 1. 데이터 — 정식 분석 499 → **915종** (+416)

| 주종 | 이전 | 현재 | 비고 |
|---|---:|---:|---|
| 와인 | 412 | 412 | 기존 완성분 유지 |
| 사케 | 101 | 101 | 기존 완성분 유지 |
| **맥주** | 3 | **100** | 라거·바이젠·IPA·스타우트·사워·트라피스트·논알콜 고루 |
| **위스키** | 20 | **100** | 스카치·버번·라이·아이리시·재패니즈·카발란·김창수·기원 |
| **전통술(영어)** | 6+2 | **100** | 외국인 대상 전면 영어 재작성 (막걸리·증류식·약주·리큐르·희석소주) |
| **브랜디** | 1 | **26** | 꼬냑 주요 하우스 + 칼바도스·아르마냑 |
| **진** | 1 | **19** | / **럼** 0→**18** / **데킬라** 0→**15** / **백주** 0→**14** / **리큐르** 0→**10** |

- 카탈로그 총 **4,561건** (정식 915 + 뼈대 3,646)
- 모든 정식 항목에 **가격대(priceBand) 부착** — 추천 후보 누락 없음
- **데이터 위생 교정**: 위스키 15종·전통술 8종의 어긋난 맛 축을 규격으로 교정 (추천 엔진 필수 조건), 국가 표기 통일, 구버전 한국어 전통술 8종은 영어판으로 병합(이미지·히트 이관) 후 옛 seed 파일에서도 제거
- 작성 방식: 멀티에이전트 워크플로우 (선정 → 작성 → **사실 검증·교정**) + 결정적 스키마 검증 스크립트. 불확실한 연도·수상은 쓰지 않거나 뭉뚱그림 (정확성 최우선 원칙)
- 기록: `data/changelog.json`에 4건 추가 (맥주/전통술/위스키/6주종)

## 2. 프론트 — 힉스필드 제외 "분위기" 구성 완료

| 작업 | 내용 |
|---|---|
| **앱별 색 테마** | `appProfile.js`의 accent/wineish가 죽은 값이었던 것을 실제 배선 — `themeCss()`로 CSS 변수(:root) 주입, 파생톤(밝음/어두움/반투명) 자동 계산. 레이더 SVG·타이머·공유카드 캔버스 같은 CSS 변수 불가 지점도 `THEME`으로 연결 |
| **브랜드 연동** | 헤더 워드마크·모토(`nameEn`/`motto`), 공유카드 상단 브랜드, 푸시 알림 제목, PWA 매니페스트(`app/manifest.js` 동적 생성, 구 정적 파일 삭제), localStorage 키 앱별 분리 |
| **영어 UI 레이어** | `lib/i18n.js` 신설 — 한국어 원문이 키인 `t()` + 금액 `fmtWon()`. **번역 482개** (UI 문구·맛 축·주종 라벨·잔 이름·서빙 안내·가격대). 전통술 앱 첫 화면 한국어 잔존 **0** 확인 |
| **영어 스캔 출력** | `lib/prompts.js` 로케일 스위치 — 전통술 앱은 Claude 분석 결과도 영어 (searchKeyword만 네이버용 한글 유지) |
| **검증** | 와인(금색·한국어)/사케(#cbb894)/전통술(영어·#c9b9a0) 스모크 테스트 + **프로덕션 빌드 성공** |

## 3. 환경 복구 (어제~오늘)

- MongoDB Atlas 재연결 — 공유기 DNS가 SRV 조회를 막아 **직접 호스트 나열 URI로 변환** (`.env.local`, 재발 시 참고)
- Anthropic 키 ✅ 유효 / Cafe24 FTP ✅ 정상 (스캔 썸네일 업로드 가능)

## 4. 남은 일 (다음 세션)

1. **네이버 검색 API 키** — 마지막 시도에서 SE05(앱에 검색 API 미등록). 새 앱을 만들어 "검색" API 선택 후 키 교체 → `POST /api/catalog/images`로 이미지 4,500건 일괄 연결
2. **힉스필드 디자인** — "촌스럽다" 클라이언트 피드백 반영 리디자인 + 주종 엠블럼. **크레딧 발생 전 보고 후 진행** 약속됨
3. 로그인 · 만 19세 인증 (클라이언트 피드백 대기)
4. Vercel **6개 앱** 배포 (`NEXT_PUBLIC_APP` = wine/sake/beer/tradition/whisky/spirits)
5. `/guide` `/plan` 내부 페이지는 출시 전 삭제 예정 (폴더째)

---

# 추가분 — 2026-08-02 (로컬 실행 점검에서 나온 수정)

## 고친 것

| # | 증상 | 원인 | 조치 |
|---|---|---|---|
| 1 | 맥주·위스키·진·럼·데킬라 앱에서 **추천이 0건** | 취향 문답이 와인 축(탄닌·당도·산도·바디)으로만 물었다. 추천 유사도는 축이 2개 이상 겹쳐야 성립하는데 맥주 축과는 '바디' 하나만 겹쳤다 | [lib/taste.js](lib/taste.js) 전면 교체 — 문항을 **주종의 맛 축에서 생성**(24개 축 문항 사전). 와인 전용 문항은 와인에서만. 예산 문항은 맥주·소주용 문구 분기. 답변은 **주종별로 분리 저장**(4개 앱이 DB 하나를 공유해 서로 덮어쓰던 문제도 차단), 기존 와인 답변은 마이그레이션으로 보존 |
| 2 | 위스키 100·브랜디 26·진 19·럼 18·데킬라 15·백주 14·리큐르 10 = **202종이 어느 앱에도 없었다** | 앱 프로필이 4개뿐이라 이 주종들은 어느 앱의 범위도 아니었다 (스캔·검색으로만 도달) | [lib/appProfile.js](lib/appProfile.js)에 **위스키 렌즈**(:5704)·**스피리츠 렌즈**(:5705) 추가 |
| 3 | 전통술 앱에서 **소주 17종이 안 보였다** | 둘러보기·추천이 `categories[0]` 하나만 봤다 | [lib/recommend.js](lib/recommend.js) 다중 주종 지원 — 주종별로 축을 따로 잡아 재고 한 줄로 합친다. 목록은 주종을 번갈아 채워 한 주종이 독차지하지 않는다 |
| 4 | 둘러보기 상단이 **뼈대(미분석) 항목**으로 채워졌다 | 뼈대에도 수확 때 추정 맛 축이 붙어 있어 "분석됨"으로 오인 | `tier` 로 판정하도록 교체. 정식 분석이 앞에 온다 |

**검증**: 와인 추천 1→8건, 맥주 0→8건(쓴맛·홉향 취향 입력 → 스톤 IPA·구스 아일랜드 IPA 등 매치 90~92), 스피리츠 둘러보기 6개 주종 균등(베일리스·바카디·봄베이 사파이어·헤네시 VS·카사미고스·연태고량주), 전통술 앱 전통주+소주 4:4, 6개 앱 동시 실행 + 테마색 각각 확인, 프로덕션 빌드 통과.

## 실행 방법 (VS Code)

**방법 1 — 실행 패널**: `Ctrl+Shift+D` → 위쪽 드롭다운에서 앱 선택 → ▶ (브라우저까지 자동으로 열림)
**방법 2 — 작업 고르기**: `Ctrl+Shift+P` → `Tasks: Run Task` → `앱 띄우기 (고르기)` → 목록에서 선택
**방법 3 — 터미널**: `` Ctrl+` `` 로 터미널 열고 아래 중 하나

```bash
npm run dev            # 🍷 와인 (보틀 렌즈)        :5700
npm run dev:sake       # 🍶 사케 렌즈               :5701
npm run dev:beer       # 🍺 비어 렌즈               :5702
npm run dev:tradition  # 🏺 Korean Spirits (영어)   :5703
npm run dev:whisky     # 🥃 위스키 렌즈             :5704
npm run dev:spirits    # 🍸 스피리츠 렌즈           :5705
```

앱마다 포트와 빌드 폴더(`.next-<앱>`)가 달라 **여섯 개를 동시에 띄워 놓고 견줄 수 있다**.
빌드는 `npm run build:beer` 처럼 같은 규칙을 쓴다.

## 앱 간 데이터 격리 (같은 날 추가로 발견·수정)

DB 하나를 여섯 앱이 함께 쓰는데 **사용자 기록이 전혀 갈라져 있지 않았다.** 맥주 앱을 열면 와인 스캔 기록 25건이 그대로 보이고, 와인 셀러가 맥주 앱의 취향 학습 재료로 쓰였다.

| 컬렉션 | 이전 | 조치 |
|---|---|---|
| `sessions` (스캔 기록) | 전 앱 공유 | 앱별 격리. 보관 상한 40개도 앱별로 센다 (한 앱이 다른 앱 기록을 밀어내던 문제) |
| `cellar` (나의 셀러) | 전 앱 공유 | 앱별 격리. 담기·수정·삭제 전부 범위 밖이면 거부 |
| 취향 학습 (`buildTasteProfile`) | 전 주종 별점 혼합 | 이 앱의 셀러만 학습 |
| 추천 중복 제외 (`ownedKeys`) | 전 앱 셀러 | 이 앱 셀러만 |
| 특가 알림 (`deals.js`) | 전 앱 셀러 | 이 앱 셀러만 — 맥주 앱이 와인 특가를 알리지 않는다 |
| 음용 적기 알림 (`drinkAlerts.js`) | 전 앱 셀러 | 이 앱 셀러만 |
| 푸시 구독 (`push_subs`) | 전 앱 공유 | 구독에 앱 표시, 발송도 그 앱 구독자에게만 |
| 미매칭 수집 (`wanted`) | 전 앱 공유 | 앱별 "다음에 채울 목록" |
| `preferences` (취향 문답) | 전 앱 공유 | 주종별 분리 (앞서 수정) |

**방식**: [lib/appProfile.js](lib/appProfile.js)에 `ownScope()`(읽기 조건)·`stamp()`(쓰기 표시)를 두고 모든 조회에 적용. 세 갈래로 본다 — ① 주종이 이 앱 범위인 기록 ② 이 앱에서 남긴 기록(판독 실패처럼 주종이 없는 것 포함) ③ 앱 표시가 없던 옛 기록은 결과 안의 주종으로 판정. 기존 기록 28건(스캔 25·셀러 3)은 전부 와인 앱 소속으로 표시 완료.

**부수 수정**: `makgeolli` 주종이 어느 앱에도 없었다(분석 프롬프트는 막걸리를 `makgeolli`로 따로 가른다) → 전통술 앱 범위에 추가. 다른 앱 항목 삭제 시 `deleted: true`로 거짓 응답하던 것 → 404.

**검증**: 맥주 앱에서 셀러에 담은 항목이 와인·스피리츠 앱에 안 보임 / 와인 앱에서 그 항목 삭제 시도 → 거부(항목 유지) / 맥주 앱에서 삭제 → 성공 / 6개 앱 스캔 기록·셀러·둘러보기 각각 자기 것만 표시 / 프로덕션 빌드 통과.

---

# 앱 구조 개편 — 엔진 공유, 껍데기 분리 (2026-08-02)

"버전마다 디자인·문구·레이아웃이 전부 달라야 한다"는 요구에 맞춰, 앱을 색만 바꾼 리스킨이 아니라 **다른 앱**으로 만들 수 있는 구조로 바꿨다. 레포는 나누지 않았다 — 코드의 90%(스캔·분석·카탈로그·추천·셀러)는 여섯 앱이 똑같이 쓰는 엔진이라, 나누면 버그 하나를 여섯 번 고쳐야 한다.

```
lib/apps/<키>.js   theme(색·서체·모서리·배경) · copy(어휘) · layout(화면 구성)
lib/appProfile.js  이 셋을 읽어 앱을 조립
그 외             엔진 — 손대지 않는다
```

| 층 | 내용 |
|---|---|
| **theme** | 색뿐 아니라 **배경 사진·장막 농도·그레인·모서리 반경·제목 서체**까지. `themeCss()`가 CSS 변수로 내려보낸다 |
| **copy** | 앱의 **어휘 사전**. `t()`가 ① 앱 어휘로 바꾸고 ② 영어 앱이면 영어로 바꾼다 |
| **layout** | **결과 화면에 무엇을 어떤 순서로 놓을지.** 적지 않은 조각은 그 앱에서 뜻이 없어 빼는 것이다 |

## 사케 앱 — 첫 사례

| 항목 | 와인 | 사케 |
|---|---|---|
| 배경 | 유리잔 림의 빛 곡선 (가넷) | **백자 오초코 · 삼나무 · 남색 안개** |
| 제목 서체 | Cormorant Garamond | **Noto Serif JP** |
| 색 | 금 #d4b278 / 밤색 | 쌀빛 #cbb894 / **남색 #22304a** |
| 모서리 | 18px | 14px (일본 기물의 단정함) |
| 어휘 | 셀러 · 빈티지 · 음용 적기 · 와인 리스트 | **술창고 · 제조시기 · 마시기 좋은 때 · 메뉴판** |
| 결과 화면 | 가격→**빈티지 비교**→맛→제원→**음용 적기** | 가격→**서빙(온도)**→**그릇**→맛→제원 (빈티지 비교 없음) |
| 엠블럼 | 병+잔 라인아트 | **도쿠리+오초코** |

맥주는 냉장고·온도 우선(빈티지·음용적기 제외), 위스키/증류주는 잔·마시는 법 우선(병입 후 익지 않으므로 음용적기 제외), 전통술은 신선도 우선으로 각각 잡아 두었다.

## 촬영 확대 (요청 반영)

줌이 아예 없었다. 세 갈래로 넣었다 — 기기 카메라 줌(`getCapabilities().zoom`, 화질 손실 없음) → 미지원이면 화면 확대 후 **찍을 때 가운데를 그만큼 잘라 낸다**(보이는 대로 찍힌다). 조작은 핀치·슬라이더·더블탭·휠.

## 알아둘 점

- 전통술 앱의 제품명은 화면에 영문만 나온다 (`Jipyeong Saeng Makgeolli`). 저장할 때 괄호 안 원어를 떼는 규칙(`splitName`) 때문인데, 한글은 `searchKeyword`에 남아 있어 쇼핑 조회는 정상이다. 외국인이 매장에서 병을 맞춰 보려면 화면에도 한글을 같이 보여 주는 편이 낫다 — 힉스필드 디자인 단계에서 함께 결정하면 좋겠다.
